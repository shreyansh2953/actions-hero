name: my_first_github_action

on:
  push:
    branches:
       - master
  workflow_dispatch:

jobs:
   hello:
      runs-on: ubuntu-latest
      steps:
         - name: hello world
           run: echo "hello world"
